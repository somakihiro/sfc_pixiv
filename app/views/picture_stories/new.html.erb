<%= form_for(@picture_story) do |f| %>
  <%= f.text_field :title, placeholder: 'タイトル' %>
  <%= f.fields_for :picture_story_orders do |order| %>
    <%= order.text_field :story, placeholder: '内容' %>
  <% end %>
  <%= f.submit '投稿' %>
<% end %>
